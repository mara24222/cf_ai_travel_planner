name = "cf_ai_travel_planner"
main = "src/index.js"
compatibility_date = "2025-12-02"

# Durable Objects (correct format for Wrangler 4.51)
[durable_objects]
bindings = [
  { name = "MEMORY", class_name = "MemoryDO" }
]

# Workers AI (correct format)
[ai]
bindings = [
  { name = "AI" }
]

# Durable Object migration
[[migrations]]
tag = "v1"
new_classes = ["MemoryDO"]
